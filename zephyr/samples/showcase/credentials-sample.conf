# Copy this file to credentials.conf and fill in the values.
CONFIG_NET_WIFI_SSID=""
CONFIG_NET_WIFI_PASSWORD=""
CONFIG_SPOTFLOW_INGEST_KEY="your-ingest-key"

# The sample normally derives a stable runtime ID from Zephyr's hardware-info
# API. This value is used as a fallback when hardware ID retrieval fails.
CONFIG_SPOTFLOW_DEVICE_ID="your-device-id"
