# Copy this file to credentials.conf and fill in your Spotflow credentials
# credentials.conf is gitignored to prevent accidental credential leaks

CONFIG_SPOTFLOW_DEVICE_ID="your-device-id"
CONFIG_SPOTFLOW_INGEST_KEY="your-ingest-key"
