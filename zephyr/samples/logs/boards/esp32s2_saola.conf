# Use PSRAM for Wi-Fi buffers to save SRAM
CONFIG_ESP_SPIRAM=y
CONFIG_ESP_WIFI_HEAP_SPIRAM=y
CONFIG_ESP32_WIFI_NET_ALLOC_SPIRAM=y

# Automatic Wi-Fi reconnection isn't enabled by default
CONFIG_ESP32_WIFI_STA_RECONNECT=y
