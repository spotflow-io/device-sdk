# I was unable to find the driver for trng so using test random generator
CONFIG_TEST_RANDOM_GENERATOR=y

# This board has no partitions configured, skipping configuration persistence
CONFIG_SPOTFLOW_SAMPLE_CONFIG_PERSISTENCE_FLASH=n
